{
  "name": "aifredo",
  "private": true,
  "version": "0.0.0",
  "description": "Personal autonomous agent hub",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r --filter=./apps/* --filter=./packages/* build",
    "dev": "pnpm -r --parallel --filter=./apps/* dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "patchedDependencies": {
      "@tigeropenapi/tigeropen@0.4.0": "patches/@tigeropenapi__tigeropen@0.4.0.patch"
    }
  }
}
